name = "fedex-fuel-surcharge-checker"
main = "src/index.mjs"
compatibility_date = "2026-05-24"

[triggers]
# Cloudflare cron uses UTC. These are Monday 08:00 and 12:00 in Asia/Shanghai.
crons = ["0 0 * * 1", "0 4 * * 1"]

[vars]
FUEL_BUFFER_RATE = "0.03"
